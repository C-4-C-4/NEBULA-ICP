---
import '../styles/global.css';
import Navbar from '../components/Navbar.astro';
import UserEdit from '../components/UserEdit.jsx';
---

<html lang="zh-cn">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>MODIFY // 修改资料</title>
</head>
<body class="bg-grid-pattern min-h-screen font-mono text-echo-dark">
    <Navbar />

    <!-- 
      核心修改点：
      1. pt-20 -> pt-6: 缩小顶部距离，让内容靠近导航栏。
      2. 移除了 items-center: 虽然 flex-col，但内部宽度已由 max-w-lg 控制，去掉可以避免某些意外拉伸。
    -->
    <main class="flex-grow flex flex-col items-center w-full pt-6 pb-12 px-4">
        
        <div class="text-center mb-10 relative z-10">
            <h1 class="text-4xl font-black mb-2">UPDATE RECORD</h1>
            
            <div class="text-sm bg-black text-white inline-flex justify-center items-center px-4 py-1 font-bold shadow-hard-sm">
                /// ACCESS CONTROL: OWNER ONLY
            </div>
        </div>

        <UserEdit client:load />

    </main>
</body>
</html>